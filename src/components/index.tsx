import { GetStaticProps } from "next"

const BookList = () => {
  return <section className="bg-white">BookList</section>
}

export default BookList

export const getStaticProps: GetStaticProps = async () => {
  try {
  } catch (error) {}

  return {
    props: {},
  }
}
